{
  "private": "true",
  "scripts": {
    "tsc": "lerna run tsc",
    "test": "lerna run test",
    "build": "lerna run tsc && lerna run build",
    "start": "node_modules/.bin/next example",
    "example": "yarn tsc && cd example && yarn dev"
  },
  "workspaces": [
    "example",
    "package"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
